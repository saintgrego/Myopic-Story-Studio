# Copy this file to .env and fill in your key.
# Read server-side only (myopic-studio/server) — never bundled into the browser.
# Never put the real key in this file — .env.example is a template that would be
# committed to source control if this project is ever git-initialized.
ANTHROPIC_API_KEY=sk-ant-...
PORT=4000
